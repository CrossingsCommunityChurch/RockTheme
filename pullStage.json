{
    "Parameters": {
        "commands": [
            "#!/bin/bash",
            "cd C:/inetpub/wwwroot/Themes/CCC_Public",
            "git fetch --all",
            "git checkout stage",
            "git reset --hard origin/stage"
        ]
    }
}